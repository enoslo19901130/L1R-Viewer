# Changelog

## 1.1.0 — operator UX (2026-07-25)

### Added
- Phase 7: `L1R.Shared` client validation, AppData settings, CLI/MCP `doctor` / `validate_client`, GETTING-STARTED
- Phase 8: `L1R.Shell` (`L1R-Viewer.exe`) single operator entry
- Phase 9: MapViewer export toolbar + 資訊 tab (PNG / portals / passability)
- Phase 10: PakBrowser numeric sprite-id search + export to default output folder
- Phase 11: `map regions` CLI; operator alignment table in OPERATOR-MANUAL
- Phase 12: assembly names `L1R-MapViewer` / `L1R-PakBrowser`; CHANGELOG / RELEASE-CHECKLIST
- Phase 13: `tests/regression.ps1`

### Changed
- Launchers resolve new and legacy exe names

## 1.0.0 — engine merge (2026-07-24/25)

- Monorepo Core + Cli + MapViewer + PakBrowser
- Headless map PNG, MCP read-only tools
- Read-only default with `--enable-edit`
