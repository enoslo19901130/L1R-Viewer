# Paste into project CLAUDE.md (optional)

## L1R-Viewer

For offline Lineage Remastered client assets (maps, sprites, pak):

1. Use MCP server **l1r-viewer** (see `.mcp.json`).
2. Or invoke skill **l1r-viewer** / instructions under `.claude/skills/l1r-viewer/`.
3. Workflow: `validate_client` → map/sprite tools. Absolute paths only.
4. Read-only. No write tools on MCP.
5. Docs: `docs/AI-INTEGRATION.md`, `docs/mcp.md`, `docs/GETTING-STARTED.md`.
